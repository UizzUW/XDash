<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XDash.Pages.DevicesPage"
             Title="Devices">
    <StackLayout Orientation="Vertical">
        <StackLayout Orientation="Horizontal" Padding="8">
            <Label Text="{Binding [Make_This_Device_Visible]}"
                   HorizontalOptions="StartAndExpand"
                   VerticalTextAlignment="Center"/>
            <Switch IsToggled="{Binding IsBeaconEnabled}"/>
        </StackLayout>

        <Label Text="{Binding [List_Of_Discovered_Dashers]}"/>
        <ListView x:Name="DashersListView" ItemsSource="{Binding Dashers}"
                  HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical" Padding="8">
                            <Label Text="{Binding Guid}" FontSize="10" />
                            <Label Text="{Binding Name}" FontSize="24" />
                            <Label Text="{Binding Ip}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>