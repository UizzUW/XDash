<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XDash.Pages.DevicesPage"
             Title="Devices">
    <StackLayout Orientation="Vertical">
        <StackLayout Orientation="Horizontal">
            <Label Text="Make this device visible"
                   TextColor="White"
                   HorizontalOptions="StartAndExpand"
                   VerticalTextAlignment="Center"/>
            <Switch IsToggled="{Binding IsBeaconEnabled}"/>
        </StackLayout>

        <Label Text="List of discovered dashers :"
               TextColor="White"/>
        <ListView x:Name="DashersListView" ItemsSource="{Binding Dashers}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Guid}" TextColor="White"/>
                            <Label Text="{Binding Name}" TextColor="White"/>
                            <Label Text="{Binding Ip}" TextColor="White"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>